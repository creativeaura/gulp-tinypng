'use strict';
var fs = require('fs');
var assert = require('assert');
var gutil = require('gulp-util');
var tinypng = require('./index');

it('should only compress png files', function (cb) {
  assert(true);
  cb();
});